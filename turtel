import random
import turtle

turtle.shape("turtle")
turtle.pencolor("green")

turtle.speed(50)
size = 5
for i in range(0,120):
    turtle.circle(size)
    turtle.left(5)
    size = size + 10

turtle.mainloop()
